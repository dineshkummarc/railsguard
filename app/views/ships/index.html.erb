<h1>Listing ships</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
  </tr>

<% @ships.each do |ship| %>
  <tr>
    <td><%=h ship.title %></td>
    <td><%=h ship.description %></td>
    <td><%= link_to 'Show', ship %></td>
    <td><%= link_to 'Edit', edit_ship_path(ship) %></td>
    <td><%= link_to 'Destroy', ship, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New ship', new_ship_path %>