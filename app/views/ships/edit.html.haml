#ship 
  - form_for(@ship, :html => { :class => 'normal', :id => "shipForm" }) do |f| 
    = render :partial => 'form', :locals => { :f => f }

%p
  = link_to 'Show', @ship  |
  = link_to 'Back', ships_path 
