<h1>New attachment</h1>

<% form_for(@attachment, :html => { :multipart => 'true' }) do |f| %>
  <%= f.error_messages %>
  <%= f.file_field :file %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', attachments_path %>